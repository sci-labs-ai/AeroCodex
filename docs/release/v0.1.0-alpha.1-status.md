# AeroCodex v0.1.0-alpha.1 release status

This is the live status record for **AeroCodex v0.1.0-alpha.1 — Research Software Alpha**. It records release engineering and repository integrity only. It does not promote formula validation status, authorize formula execution, expand scientific or life-support scope, or make a certification claim.

## Release target and source revision

| Field | Value |
|---|---|
| Release target | `AeroCodex v0.1.0-alpha.1 — Research Software Alpha` |
| Base branch | `release/v0.1.0-alpha.1` |
| Base commit | `ffcc2b218220cf705d4673f3b44b45c929f3a65d` (`ffcc2b2 Merge pull request #46 from sci-labs-ai/m00/promotion-template`) |
| Source revision policy | Manifest pins the reviewed base commit; the eventual release artifact commit must be recorded by a later authorized packaging/publication batch. |
| Current implementation batch | Release source of truth and repository integrity |

The formal release audit and formal release plan requested for `docs/release/` were not present in the repository, attachment, or available project context. Their contents have not been invented; they must be supplied and added separately.

## Pre-change source-of-truth audit

| Claim | Sources found before this batch | Pre-change state |
|---|---|---|
| Project/package version | Root `[workspace.package]` in `Cargo.toml`; inherited package manifests; `docs/phase_0_001/version_lock_audit.md`; README | Cargo packages were locked to `0.0.1`; the requested formal release target was absent. |
| Release status/channel | README; `docs/beta1/release_concept.md`; `docs/beta1/release_testing.md`; CLI `release_channel()`; `docs/assurance/merge_and_release_policy.md` | `beta1-concept`, research/preliminary-design, not public-release approved; no `v0.1.0-alpha.1` release contract existed. |
| Formula identifier | `equation-batches/*.tsv`; `generated/formula_registry.json`; `docs/registry/m00_formula_id_map.csv`; formula sidecars; CLI dispatch specs | Generated registry canonical IDs plus legacy aliases were governed; CLI specs used legacy `formula_vault.*` IDs internally. |
| Formula status | `validation/status_vocabulary.*`; validation cards/source seeds; equation-batch rows; generated registry; `VerificationStatus` in core | All 152 generated-registry rows were `research_required`. |
| Execution policy | `docs/assurance/formula_status_gate_policy.md`; formula registry generator; generated JSON/Rust; CLI status gate | `research_required` mapped to `blocked`; normal execution required `implementation_verified`. |
| Public executability | Formula status policy; generated registry; CLI integration tests; README and Beta 1 quickstart | Registry and tests denied execution, while older README/Beta 1 examples claimed successful public execution. |
| Runtime symbol | Equation batches; runtime-resolution TSV; generated registry; CLI `FormulaSpec` table | Runtime-symbol presence documented implementation linkage but did not authorize execution. |
| Formula counts | `docs/roadmap/research_readiness_counts.md`; public-alpha dashboard; generated registry; equation inventory; CLI source | Registry/equation-batch count was 152; M00 runtime-link candidates were 27; CLI documentation claimed 10 accessible formulas while CLI source contained 12 dispatch specs; default executable count was zero. |
| Checksum/integrity status | `checksums/SHA256SUMS`; README and pull-request template `sha256sum` command; `ARTIFACT_MANIFEST.md` | Manifest claimed the current public set but omitted 81 tracked files, held 8 stale hashes, and had no blocking CI verification. |
| Generated-artifact status | Deterministic generation policy; formula-registry generator/check; equation-batch report `--check`; checked-in `generated/` files | Individual read-only checks existed, but no combined missing-file/drift/dirty-process gate existed. |

## Contradictions recorded before resolution

1. The requested `v0.1.0-alpha.1` release identity conflicted with Cargo `0.0.1`, the human `Phase 0.001` label, and the `beta1-concept` CLI channel. This batch establishes a separate release-manifest version and does not change Cargo packaging metadata or CLI release identity.
2. README and `docs/beta1/cli_quickstart.md` described ten M00 formulas as an executable surface and showed a successful `research_required` run. The generated registry, status-gate policy, CLI gate implementation, and integration tests require all such runs to fail closed.
3. The CLI source held twelve dispatch specs (ten canonical-unit plus two angle conversions), while Beta 1 verification/docs claimed a supported count of ten. Dispatch linkage and self-check coverage are now distinguished from public executability; the release manifest records twelve linked records and zero public executable formulas.
4. `ARTIFACT_MANIFEST.md` called `checksums/SHA256SUMS` the current public file set, but the checksum manifest omitted 81 tracked files added after its last refresh and had 8 stale entries.
5. The documented `sha256sum -c checksums/SHA256SUMS` command was Unix-specific. On the inspected Windows checkout, Git `core.autocrlf=true` converted the checksum manifest and governed text files to CRLF, causing the raw tool to treat all 419 manifest paths as ending in `\r`; an independent canonical audit then found 408 line-ending-only matches, 8 real stale hashes, and 3 raw-byte matches.

## Checksum baseline and resolution

| Mismatch class | Count | Affected files | Classification | Resolution |
|---|---:|---|---|---|
| Manifest paths unreadable by documented raw command on Windows | 419 | Every pre-change checksum entry | Generated-artifact/procedure inconsistency caused by CRLF checkout of `SHA256SUMS` | Replaced governing procedure with dependency-free Rust verification that parses CRLF and uses canonical LF hashing for UTF-8 text. |
| Checkout line endings only | 408 | Pre-change text entries whose raw checkout bytes differed but whose canonical LF digests matched | Intentional Git `core.autocrlf=true` checkout representation, not content drift | Canonical LF digests retained; no content or scientific change. |
| Raw-byte matches | 3 | `assets/aerocodex_patch.png`; `source_material/new_thinfilm/blss_thinfilm_latex_source.zip`; `source_material/new_thinfilm/blss_thinfilm_report.pdf` | No mismatch after the raw procedure path issue is removed | Digests retained. |
| Stale checksum | 8 | `.github/workflows/ci.yml`; `README.md`; `crates/aero-codex-cli/src/main.rs`; `crates/aero-codex-cli/tests/cli.rs`; `docs/beta1/cli_quickstart.md`; `docs/index.md`; `docs/roadmap/public_alpha_readiness_dashboard.md`; `xtask/src/main.rs` | Missing checksum updates for already-reviewed tracked changes | Reviewed against current policy/code and refreshed to canonical content; batch-modified entries were hashed only after final review. |
| Tracked file absent from checksum manifest | 81 | Research-readiness workflow/configuration/docs/schema/fixtures/goldens and generated/xtask files added after checksum commit `77c2e23` | Missing checksum coverage, including four generated artifacts | Reviewed by artifact family, added to complete coverage, and thereafter enforced by `verify-checksums`. |

The 81 omitted tracked paths were: `.github/ISSUE_TEMPLATE/research_readiness_task.md`, `.github/labels/research_readiness_labels.yaml`, `.github/workflows/research-readiness.yml`; `docs/architecture/{formula_cli_contract.md,formula_module_architecture.md,formula_registry_contract.md}`; `docs/assurance/{deterministic_generation_policy.md,formula_error_code_catalog.csv,formula_id_policy.md,formula_research_readiness_gate.yaml,formula_schema_sidecars.md,formula_status_gate_policy.md,m00_promotion_checklist.md,promotion_packet_template.md,public_wording_guardrails.md,source_licensing_checklist.md}`; `docs/contributing/agent_pr_check.md`; `docs/development/{ci_gates.md,toolchain.md}`; `docs/registry/{m00_deferred_candidate_backlog.csv,m00_formula_id_map.csv}`; `docs/research_alpha/{cli_formula_quickstart.md,json_contract.md}`; `docs/roadmap/{equation_batch_status_report.md,formula_registry_generation.md,research_readiness_agent_decision_packet.md,research_readiness_counts.md,research_readiness_dependency_conflict_matrix.md,research_readiness_execution_waves.md,research_readiness_formal_plan.md,research_readiness_queue_order.md,reviewer_checklists_v0_7_2.md}`; `docs/templates/{formula_promotion_packet_template.yaml,formula_sidecar_template.yaml}`; `docs/testing/formula_registry_fixtures_and_golden_outputs.md`; `formula-schemas/README.md`; `formula-schemas/m00/angle/deg_to_rad.yaml`; `formula-vault/contracts/README.md`; all four tracked `generated/` artifacts; three `schemas/formula_*.json` files; `scripts/agent_pr_check.sh`; eight `tests/fixtures/equation_batch/*` files; four `tests/fixtures/formula_registry/*` files; nine `tests/golden/cli/*` files; two `tests/golden/equation_batch/*` files; three `tests/golden/formula_registry/*` files; `validation/cards/README.md`; six `xtask/src/equation_batch/*.rs` files; and three `xtask/src/formula_registry/*.rs` files.

## Completed tasks

- Added a machine-readable TOML release manifest with pinned base commit, release tier/version, validation and execution posture, documentation/evidence references, formula counts, twelve dispatch-linked formula records, runtime symbols, and explicit `public_executable=false` declarations.
- Added strict release-manifest parsing and validation, duplicate-ID checks, status/policy compatibility checks, executable-evidence checks, path/reference checks, and cross-checks against the generated formula registry.
- Added dependency-free, cross-platform checksum verification with strict syntax/path ordering, complete repository coverage, missing/changed-file detection, and canonical text line endings.
- Added a combined generated-artifact check for the equation-batch status report and formula registry, including required-file checks and before/after repository snapshots.
- Added blocking CI gates and updated contributor/developer commands.
- Corrected top-level and Beta 1 CLI documentation so dispatch/self-check linkage is not described as public executability.
- Added unit tests for the requested release-manifest, checksum, and generated-drift failure modes.
- Fixed two pre-existing Windows-only test blockers exposed by the full suite: Unix-style repository-absolute paths are now rejected consistently, and temporary equation-batch package roots use directory junctions without requiring symlink privilege.
- Fixed a pre-existing equation-batch probe dependency gap by declaring workspace crates referenced directly by generated test expressions; the A5 probe now declares `aero-codex-core` instead of relying on an inaccessible transitive dependency.
- Refreshed checksums only after reviewing affected files and mismatch causes.

## Modified files

Final file list is recorded here after diff review:

- `.github/PULL_REQUEST_TEMPLATE.md`
- `.github/workflows/ci.yml`
- `.gitignore`
- `README.md`
- `checksums/README.md`
- `checksums/SHA256SUMS`
- `docs/beta1/cli_quickstart.md`
- `docs/beta1/release_concept.md`
- `docs/beta1/release_testing.md`
- `docs/development/ci_gates.md`
- `docs/index.md`
- `docs/release/v0.1.0-alpha.1-status.md`
- `docs/release/v0.1.0-alpha.1.toml`
- `docs/research_alpha/json_contract.md`
- `docs/roadmap/public_alpha_readiness_dashboard.md`
- `docs/roadmap/research_readiness_counts.md`
- `docs/roadmap/versioning.md`
- `docs/testing/friend_test_expected_output.md`
- `docs/testing/friend_test_quickstart.md`
- `scripts/agent_pr_check.sh`
- `scripts/friend_test_local.ps1`
- `scripts/friend_test_local.sh`
- `xtask/src/checksums.rs`
- `xtask/src/equation_batch/generate.rs`
- `xtask/src/equation_batch/manifest.rs`
- `xtask/src/equation_batch/report.rs`
- `xtask/src/formula_registry/check.rs`
- `xtask/src/formula_registry/mod.rs`
- `xtask/src/formula_registry/rust.rs`
- `xtask/src/generated_artifacts.rs`
- `xtask/src/main.rs`
- `xtask/src/release_manifest.rs`

## Verification commands and test results

| Command | Result |
|---|---|
| `git status` | Baseline PASS: clean branch before edits. Pre-commit PASS: exactly the 32 documented paths were staged, with no other unstaged or untracked change. |
| `git branch --show-current` | PASS: `release/v0.1.0-alpha.1`. |
| `git log -1 --oneline` | PASS: `ffcc2b2 Merge pull request #46 from sci-labs-ai/m00/promotion-template`. |
| `cargo metadata --no-deps` | PASS: 14 workspace packages, all inheriting version `0.0.1`. |
| `sha256sum -c checksums/SHA256SUMS` | Environment/procedure FAIL before edits: command absent from PowerShell; Git-bundled raw command then failed all 419 paths because CRLF left `\r` in parsed names. |
| `cargo fmt --all -- --check` | PASS. |
| `cargo check --workspace` | PASS. |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | PASS. |
| `cargo test --workspace --all-features` | PASS: all workspace unit, integration, and documentation tests; `xtask` ran 83 tests. |
| `cargo doc --workspace --all-features --no-deps` | PASS. |
| `cargo run -p xtask -- verify-checksums` | PASS: complete governed-file coverage with canonical cross-platform text hashing. |
| `cargo run -p xtask -- verify-release-manifest` | PASS: 12 dispatch-linked records, 0 public executable formulas, release policy blocked. |
| `cargo run -p xtask -- verify-generated` | PASS: 4 required artifacts, 0 stale, 0 missing, 0 repository drift. |
| `cargo run -p xtask -- verify --all` | PASS: all repository governance checks, including the release manifest. |
| `cargo run -p xtask -- equation-batch plan --all-manifests --json` plus JSON parser | PASS. |
| `cargo run -p xtask -- equation-batch report --all-manifests --out generated/equation_batch_status_report.json --check` | PASS. |
| `cargo run -p xtask -- equation-batch verify --all-manifests --output-dir <temporary-directory> --json --check` | PASS after the probe dependency fix: 8/8 manifests and 152/152 generated checks passed. |
| `cargo run -p xtask -- formula-registry check` | PASS. |
| `cargo run -p xtask -- dependency-policy` | PASS. |
| `cargo run -p aero-codex-cli -- version --json` | PASS; reports Cargo `0.0.1` and channel `beta1-concept`, preserving the recorded identity blocker. |
| `cargo run -p aero-codex-cli -- formula status-report --json` | PASS; reports 152 `research_required`/blocked registry entries. |
| `cargo run -p aero-codex-cli -- self-check --json` | PASS: 14 checks, 0 failures; this is not public execution authorization. |
| `bash scripts/agent_pr_check.sh` | PASS: all 17 local handoff steps. |

The host initially had no `cargo` executable, so verification used a temporary Rust stable toolchain with `rustfmt` and `clippy` under the system temporary directory. The first full workspace test exposed environment/Windows test-harness issues: `python3` was not on `PATH`, Unix-style absolute-path rejection was platform-dependent, and Windows symlink creation required unavailable privilege. A temporary `python3.exe` alias enabled the existing fixture command, and the two repository defects were fixed without weakening assertions. The repository-wide generated probe then exposed an A5 `aero-codex-core` direct-dependency omission; the generator and regression coverage were corrected, and all 152 probe checks passed.

## Unresolved blockers and human review

- The formal release audit and formal release plan are unavailable and must be supplied separately.
- Cargo package version `0.0.1` and CLI channel `beta1-concept` do not yet match the release-manifest identity. Changing them belongs to a separately authorized versioning/packaging batch.
- Release artifact source commit, packaging, signing, tagging, publication, and distribution are not authorized in this batch.
- No formula is publicly executable. Any future promotion requires traceable validation evidence and a separately authorized formula-promotion batch.
- All M07 material remains quarantined and blocked; life-support and scientific scope are unchanged.

## Next authorized batch

No later implementation batch is authorized by this work. The recommended next separately approved batch is to ingest the supplied formal release audit/plan and reconcile package/CLI release identity with the manifest before any packaging work. Formula promotion, broader CLI execution, packaging, signing, tagging, or publication must remain separate.

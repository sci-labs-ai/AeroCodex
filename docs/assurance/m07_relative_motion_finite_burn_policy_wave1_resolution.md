# A31 external M07 relative-motion and finite-burn scalar policy Wave 1

This metadata-only wave records terminal dispositions for the first 40 source-ordered rows in the governed relative-motion and finite-burn scalar policy backlog. It does not add runtime kernels, formula nodes, fixtures, source imports, public API claims, parity claims, certification evidence, or operational-readiness claims.

Selected rows: `PORT_STATUS_RELEASE_GATE.csv:row_0401` through `PORT_STATUS_RELEASE_GATE.csv:row_0971`.

The selected rows remain blocked until explicit relative-motion frame contracts, local-frame conventions, linearization-domain policy, finite-burn vehicle model policy, rocket-equation unit/domain policy, source registry, and independent validation oracles are approved.

Resolution manifest: `formula-vault/resolutions/m07_relative_motion_finite_burn_policy_wave1.tsv`.

Verifier: `cargo run -p xtask -- verify formula-vault`.

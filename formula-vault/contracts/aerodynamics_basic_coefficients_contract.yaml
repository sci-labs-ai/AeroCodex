# Governed existing-runtime equation contract backfill.
# Metadata and compiler-probe contract only; no runtime kernel is changed.

schema_version: formula_vault_runtime_contract.v1
record_status: existing_runtime_contract_research_required
contract_id: formula_vault.aerodynamics.basic_coefficients
title: Basic aerodynamic force and coefficient contracts
validation_status: research_required
primary_validation_card: validation/cards/aerodynamics_basic_coefficients.yaml
primary_source_seed: validation/source_registry/aerodynamics_basic_coefficients.yaml
runtime_policy:
  finite_inputs_required: true
  invalid_domain_returns_error: true
  nonfinite_outputs_rejected: true
  ieee754_f64_rounding_applies: true
  deterministic_same_build_platform: true
evidence_policy:
  compiler_verified_runtime_symbols: true
  bounded_contract_probes: true
  validation_status_upgrade: blocked_without_source_and_tolerance_review
non_claims:
  no_certification_evidence: true
  no_operational_readiness_claim: true
  no_external_reference_parity_claim: true
  no_full_equation_inventory_completion_claim: true
formulas:
  - formula_id: formula_vault.aerodynamics.forces.dynamic_pressure
    runtime_symbol: dynamic_pressure
    output_variable: dynamic_pressure_pa
    test_strategy: tolerance
    contract_state: compiler_probe_ready
    failure_policy: reject_invalid_domain_and_nonfinite_results
    numerical_policy: bounded_f64_contract_probe
  - formula_id: formula_vault.aerodynamics.forces.lift
    runtime_symbol: lift
    output_variable: lift_n
    test_strategy: tolerance
    contract_state: compiler_probe_ready
    failure_policy: reject_invalid_domain_and_nonfinite_results
    numerical_policy: bounded_f64_contract_probe
  - formula_id: formula_vault.aerodynamics.forces.drag
    runtime_symbol: drag
    output_variable: drag_n
    test_strategy: tolerance
    contract_state: compiler_probe_ready
    failure_policy: reject_invalid_domain_and_nonfinite_results
    numerical_policy: bounded_f64_contract_probe
  - formula_id: formula_vault.aerodynamics.coefficients.lift_coefficient
    runtime_symbol: lift_coefficient
    output_variable: lift_coefficient
    test_strategy: tolerance
    contract_state: compiler_probe_ready
    failure_policy: reject_invalid_domain_and_nonfinite_results
    numerical_policy: bounded_f64_contract_probe
  - formula_id: formula_vault.aerodynamics.coefficients.drag_coefficient
    runtime_symbol: drag_coefficient
    output_variable: drag_coefficient
    test_strategy: tolerance
    contract_state: compiler_probe_ready
    failure_policy: reject_invalid_domain_and_nonfinite_results
    numerical_policy: bounded_f64_contract_probe
  - formula_id: formula_vault.aerodynamics.drag.induced_drag_coefficient
    runtime_symbol: induced_drag_coefficient
    output_variable: induced_drag_coefficient
    test_strategy: tolerance
    contract_state: compiler_probe_ready
    failure_policy: reject_invalid_domain_and_nonfinite_results
    numerical_policy: bounded_f64_contract_probe
